`git clone https://github.com/go-vgo/Mingw.git` in the root of C:

Then set the enviroment variables:

```
set GOPATH=c:\users\kalebo\repos\gocode
set PATH=C:\mingw\bin;%PATH%
```

Make sure to `go get` the dependencies